import { DEFAULT_VIEWPORT, INITIAL_VIEWPORTS } from '@storybook/addon-viewport';

export const ViewportsParameters = {
  viewport: {
    defaultViewport: DEFAULT_VIEWPORT,
    viewports: INITIAL_VIEWPORTS,
  },
};
